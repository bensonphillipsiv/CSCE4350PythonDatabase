This program is written in python and uses MySQL server

Server Properties defined in the create_db.py:
host="localhost",
user="root",
passwd="1234"

To begin first run create_db.py
Then run introMenu.py

Then you can navigate through all of the menus to use the built in functions